name: NexusCRM Continuous Integration

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    name: Unit & Integration Tests (Python 3.12)
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Verify Codebase Scale (>50k LOC)
      run: |
        python scripts/count_loc.py

    - name: Run Test Suite
      run: |
        python -c "from nexus.database.connection import get_db_session; from nexus.database.migrations import run_migrations; from nexus.database.seed_data import seed_database; conn=get_db_session().__enter__(); run_migrations(conn); seed_database(conn, 25)"
        python -m unittest discover -s tests -p "test_*.py" -v
